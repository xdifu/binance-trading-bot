Utility scripts for operations and diagnostics (time sync, connectivity, health checks).

- Keep scripts here out of the main runtime path.
- Run them with the project root as working directory (they add root to `PYTHONPATH`).
